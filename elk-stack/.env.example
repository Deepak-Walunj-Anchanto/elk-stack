# Copy to .env and fill with your values. Never commit .env to git.
# Required for Kibana (kibana_system user password from elasticsearch-reset-password -u kibana_system)
ELASTICSEARCH_PASSWORD=

# Required for Logstash: writer user (from Kibana Dev Tools _security/user/logstash_writer)
LOGSTASH_WRITER_PASSWORD=

# Required for Logstash monitoring (logstash_system user from elasticsearch-reset-password -u logstash_system)
LOGSTASH_SYSTEM_PASSWORD=

# Optional: Elasticsearch API key (encoded value from POST /_security/api_key) for backend/scripts
# ELASTICSEARCH_API_KEY=
